# Interlink Properties website — project instructions

- Do not use git worktrees for this repo. Work directly in this checkout, commit on a feature branch, push, and open a draft PR as usual.
- This directory (`website/`) is the actual site code. The parent repo root also contains unrelated personal/business files (photos, GTM/tracking config, notes) — treat `website/` as the project scope for site changes.
